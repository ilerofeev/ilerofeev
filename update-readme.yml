name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: ilerofeev/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ secrets.f470588dedf6034db18d344975d65f96c9f0ae5c }}
